export function encryptSecret(value: string): string {
  // Placeholder. Next phase will use Electron safeStorage.
  return value
}

export function decryptSecret(value: string): string {
  // Placeholder. Next phase will use Electron safeStorage.
  return value
}
